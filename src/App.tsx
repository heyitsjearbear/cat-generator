import { FC } from "react";
import "./App.css";
import AuthCards from "./AuthCards";

const App: FC = () => {
  return (
    <div className="app-container">
      <AuthCards />
    </div>
  );
};

export default App;
